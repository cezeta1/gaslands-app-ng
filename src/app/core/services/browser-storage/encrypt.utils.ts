// import CryptoJS, { AES } from 'crypto-js';

// export const encryptAES = (data: string, key: string) => AES.encrypt(JSON.stringify(data), key).toString();
// export const decryptAES = (cipher: string, key: string) => AES.decrypt(cipher, key).toString(CryptoJS.enc.Utf8);